Run the synthetic-data loop with `python models/retrain.py` (uses `ingestion.synthetic_data.generate_all` over STEG districts); run against real validated history with `python -m models.retrain_historical --dataset results/datasets/rooftop_actual_15min.csv`; optional `--weather` flag supplies a separate weather CSV when the measurement file lacks `ghi_wm2`, `temp_c`, `cloud_cover_pct`.